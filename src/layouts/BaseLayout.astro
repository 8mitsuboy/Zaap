---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

interface Props {
  pageTitle: string;
}
const { pageTitle } = Astro.props;
---

<html lang="ja" class="dark">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <meta name="google-site-verification" content="Pecayi8dF0FX3zFUyinQIqdla7PtY4Vw_O3TKvXOygs" />
    <meta property="og:site_name" content="Zaap" />
    <script type="application/ld+json" is:inline>
      {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "Zaap",
        "url": "https://zaap-dev.netlify.app"
      }
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@400;500;700&display=swap" rel="stylesheet" />
    <title>{pageTitle}</title>
    <!-- FOUC防止: ページ描画前にテーマを適用 -->
    <script is:inline>
      const theme = (() => {
        const stored = localStorage?.getItem("theme") ?? "";
        if (["dark", "light"].includes(stored)) return stored;
        if (window.matchMedia("(prefers-color-scheme: dark)").matches) return "dark";
        return "light";
      })();
      if (theme === "light") {
        document.documentElement.classList.remove("dark");
      } else {
        document.documentElement.classList.add("dark");
      }
      window.localStorage.setItem("theme", theme);
    </script>
  </head>

  <body class="min-h-screen flex flex-col bg-bzine-light-bg dark:bg-bzine-bg">
    <Header />
    <main class="max-w-4xl mx-auto w-full px-6 pt-24 pb-12 flex-1">
      <h1 class="font-heading font-bold text-3xl md:text-4xl mb-6 text-bzine-light-text dark:text-bzine-text">{pageTitle}</h1>
      <slot />
    </main>
    <Footer />
  </body>
</html>
