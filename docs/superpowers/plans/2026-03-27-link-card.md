# LinkCard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `LinkCard.astro` コンポーネントを作成し、外部URLをアニメーション付きカードで表示できるようにする。あわせて既存記事をMDXに変換して使用する。

**Architecture:** AstroのSSGビルド時にfetchでリンク先HTMLの`<title>`を取得し、タイトルを自動設定する。ホバー時にキラキラアニメーションと「参考にさせていただきます🙏」バッジを表示するCSSアニメーションをコンポーネント内に閉じ込める。

**Tech Stack:** Astro, @astrojs/mdx, Tailwind CSS (bzineカラートークン)

---

### Task 1: MDXインテグレーションの追加

**Files:**
- Modify: `astro.config.mjs`

- [ ] **Step 1: @astrojs/mdx をインストール**

```bash
pnpm add @astrojs/mdx
```

Expected output: `@astrojs/mdx` がpackage.jsonのdependenciesに追加される

- [ ] **Step 2: astro.config.mjs にmdxインテグレーションを追加**

```js
// @ts-check
import { defineConfig, passthroughImageService } from "astro/config";
import preact from "@astrojs/preact";
import mdx from "@astrojs/mdx";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  site: "https://bzine.netlify.app/",
  integrations: [preact(), mdx()],
  image: {
    service: passthroughImageService(),
  },
  vite: {
    plugins: [tailwindcss()],
  },
});
```

- [ ] **Step 3: devサーバーが正常起動することを確認**

```bash
pnpm run dev
```

Expected: エラーなく起動する

- [ ] **Step 4: コミット**

```bash
git add astro.config.mjs package.json pnpm-lock.yaml
git commit -m "@astrojs/mdxインテグレーションを追加"
```

---

### Task 2: LinkCard.astro コンポーネントの作成

**Files:**
- Create: `src/components/LinkCard.astro`

- [ ] **Step 1: LinkCard.astro を作成**

```astro
---
interface Props {
  url: string;
  title?: string;
}

const { url, title: titleProp } = Astro.props;

// "DOCS · BLENDER.ORG" 形式に整形（サブドメインがあれば分離）
const hostname = new URL(url).hostname.replace(/^www\./, '');
const parts = hostname.split('.');
const subdomain = parts.length > 2 ? parts[0].toUpperCase() : null;
const mainDomain = (parts.length > 2 ? parts.slice(1) : parts).join('.').toUpperCase();
const domainLabel = subdomain ? `${subdomain} · ${mainDomain}` : mainDomain;

let title = titleProp;

if (!title) {
  try {
    const res = await fetch(url, { signal: AbortSignal.timeout(5000) });
    const html = await res.text();
    const match = html.match(/<title[^>]*>([^<]+)<\/title>/i);
    title = match ? match[1].trim() : url;
  } catch {
    title = url;
  }
}
---

<div class="link-card-wrap">
  <div class="thanks-badge">参考にさせていただきます 🙏</div>
  <span class="spark s1" aria-hidden="true">✦</span>
  <span class="spark s2" aria-hidden="true">⋆</span>
  <span class="spark s3" aria-hidden="true">✦</span>
  <span class="spark s4" aria-hidden="true">⋆</span>
  <span class="spark s5" aria-hidden="true">✦</span>
  <a href={url} target="_blank" rel="noopener noreferrer" class="link-card">
    <div class="link-card-body">
      <div class="link-card-domain">{domainLabel}</div>
      <div class="link-card-title">{title}</div>
    </div>
    <svg class="link-card-icon" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
      <path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6"/>
      <polyline points="15 3 21 3 21 9"/>
      <line x1="10" y1="14" x2="21" y2="3"/>
    </svg>
  </a>
</div>

<style>
  .link-card-wrap {
    position: relative;
    padding-top: 36px;
    margin: 1rem 0;
  }

  /* ありがとうバッジ */
  .thanks-badge {
    position: absolute;
    top: 4px;
    right: 12px;
    font-size: 0.7rem;
    font-weight: 700;
    color: #17171c;
    background: #c4ad6c;
    padding: 2px 10px;
    border-radius: 99px;
    opacity: 0;
    transform: translateY(4px);
    transition: opacity 0.25s, transform 0.25s;
    pointer-events: none;
    white-space: nowrap;
    z-index: 10;
  }
  .thanks-badge::after {
    content: '';
    position: absolute;
    bottom: -4px;
    right: 16px;
    border: 4px solid transparent;
    border-top-color: #c4ad6c;
  }
  :global(html:not(.dark)) .thanks-badge {
    color: #fefdfb;
    background: #9a8a50;
  }
  :global(html:not(.dark)) .thanks-badge::after {
    border-top-color: #9a8a50;
  }

  /* スパークル */
  .spark {
    position: absolute;
    pointer-events: none;
    font-size: 0.9rem;
    color: #c4ad6c;
    opacity: 0;
    top: 36px;
    z-index: 10;
  }
  :global(html:not(.dark)) .spark { color: #9a8a50; }
  .s1 { left: 10%; }
  .s2 { left: 28%; }
  .s3 { left: 50%; }
  .s4 { left: 70%; }
  .s5 { left: 88%; }

  /* カード本体 */
  .link-card {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.7rem 0.9rem;
    border-radius: 10px;
    background: #17171c;
    border: 1px solid #2e2e36;
    position: relative;
    overflow: hidden;
    text-decoration: none;
    transition: border-color 0.3s, background 0.3s;
  }
  :global(html:not(.dark)) .link-card {
    background: #fefdfb;
    border-color: #e4e0d6;
  }
  .link-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, #c4ad6c, transparent);
  }
  :global(html:not(.dark)) .link-card::before {
    background: linear-gradient(90deg, #9a8a50, transparent);
  }

  .link-card-body {
    min-width: 0;
    flex: 1;
  }

  .link-card-domain {
    font-size: 0.72rem;
    color: #7e7e8a;
    letter-spacing: 0.05em;
    margin-bottom: 3px;
  }
  :global(html:not(.dark)) .link-card-domain { color: #6e6b62; }

  .link-card-title {
    font-size: 0.85rem;
    font-weight: 600;
    color: #dcdce2;
    transition: color 0.3s;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  :global(html:not(.dark)) .link-card-title { color: #1a1a1e; }

  .link-card-icon {
    flex-shrink: 0;
    color: #c4ad6c;
  }
  :global(html:not(.dark)) .link-card-icon { color: #9a8a50; }

  /* ホバー: バッジ */
  .link-card-wrap:hover .thanks-badge {
    opacity: 1;
    transform: translateY(0);
  }

  /* ホバー: スパークルアニメーション */
  .link-card-wrap:hover .spark {
    animation: sparkfly 0.7s ease forwards;
  }
  .link-card-wrap:hover .s1 { animation-delay: 0s; }
  .link-card-wrap:hover .s2 { animation-delay: 0.08s; }
  .link-card-wrap:hover .s3 { animation-delay: 0.04s; }
  .link-card-wrap:hover .s4 { animation-delay: 0.12s; }
  .link-card-wrap:hover .s5 { animation-delay: 0.06s; }

  @keyframes sparkfly {
    0%   { opacity: 0; transform: translateY(0) scale(0.5); }
    30%  { opacity: 1; }
    100% { opacity: 0; transform: translateY(-28px) scale(1.1) rotate(20deg); }
  }

  /* ホバー: カード */
  .link-card-wrap:hover .link-card {
    border-color: #c4ad6c55;
    background: #1a1a20;
  }
  :global(html:not(.dark)) .link-card-wrap:hover .link-card {
    border-color: #9a8a5055;
    background: #f2efe6;
  }

  /* ホバー: タイトル */
  .link-card-wrap:hover .link-card-title {
    color: #c4ad6c;
  }
  :global(html:not(.dark)) .link-card-wrap:hover .link-card-title {
    color: #9a8a50;
  }
</style>
```

- [ ] **Step 2: devサーバーでコンポーネントのimportエラーがないか確認**

```bash
pnpm run dev
```

Expected: コンパイルエラーなし

- [ ] **Step 3: コミット**

```bash
git add src/components/LinkCard.astro
git commit -m "LinkCard.astroコンポーネントを追加"
```

---

### Task 3: 記事をMDXに変換してLinkCardを使用

**Files:**
- Rename: `src/pages/posts/2026-03-27-shading/index.md` → `index.mdx`
- Delete: `src/pages/posts/2026-03-27-shading/index.md`

- [ ] **Step 1: index.md を index.mdx にリネーム**

```bash
mv src/pages/posts/2026-03-27-shading/index.md src/pages/posts/2026-03-27-shading/index.mdx
```

- [ ] **Step 2: index.mdx の先頭にimportを追加し、インラインリンクをLinkCardに置き換える**

ファイル冒頭（frontmatterの直後）に追加：

```mdx
---
layout: ../../../layouts/MarkdownPostLayout.astro
title: "パーティクルで生成した複数のヘアーをランダムに色分けしたい"
pubDate: 2026-03-27
description: ""
author: "はちみつぼーい"
tags: ["Blender", "シェーディング", "マテリアル", "パーティクル", "ヘアー"]
image:
  url: /posts/2026-03-27-shading/thumbnail.png
  alt: "パーティクル（ヘアー）にランダムな色をつけるシェーダーノードの設定"
references:
  - url: https://www.udemy.com/course/blenderaquarium/learn/lecture/52968061#content
    label: "アクアリウム_24"
---

import LinkCard from '../../../components/LinkCard.astro'
```

そして記事内の以下の行：

```
[公式ドキュメント](https://docs.blender.org/manual/ja/dev/render/shader_nodes/input/object_info.html)
```

を以下に置き換える：

```mdx
<LinkCard url="https://docs.blender.org/manual/ja/dev/render/shader_nodes/input/object_info.html" />
```

- [ ] **Step 3: ブラウザで記事ページを開いてLinkCardが表示されることを確認**

`http://localhost:4321/posts/2026-03-27-shading/` を開く。

Expected:
- カードが表示されている
- ホバーでキラキラ＋バッジが出る
- タイトルが自動取得されている（「オブジェクト情報 — Blender マニュアル」など）
- ダーク／ライト切り替えで両モードが正常に見える

- [ ] **Step 4: コミット**

```bash
git add src/pages/posts/2026-03-27-shading/index.mdx
git commit -m "記事をMDXに変換してLinkCardを使用"
```
